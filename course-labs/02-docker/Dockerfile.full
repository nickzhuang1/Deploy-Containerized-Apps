FROM python:3.10-slim

WORKDIR /app

COPY requirements.full.txt .
RUN pip install --no-cache-dir -r requirements.full.txt

COPY app-with-db.py app.py

EXPOSE 5000

RUN useradd -m appuser
USER appuser

CMD ["gunicorn", "--bind", "0.0.0.0:5000", "--workers", "2", "app:app"]
