apiVersion: v1
kind: Service
metadata:
  name: flask-demo-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: flask-demo
  ports:
  - port: 80            # ClusterIP port
    targetPort: 5000    # 容器 port
    nodePort: 30500     # 從外部用 <NodeIP>:30500 存取
